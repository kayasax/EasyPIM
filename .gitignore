############################
# EasyPIM Git Ignore
# Cleanup enhancements (items 1,3,11,12): ignore logs/exports/artifacts, enforce text normalization
############################

# Runtime / diagnostic logs
LOGS/
*.ps1.log
*.log

# Exported data (retain curated samples via whitelist below)
EXPORTS/

# Test run outputs
TestResults/
coverage/
debug/

# Build / publish artifacts
publish/
build/output/
scripts/

# Large temporary / scratch test folders (legacy)
TEST/
TESTS/

# Non-production examples (retain README for context)
examples/
!examples/README.md

# CSV exports by default (whitelist config & doc samples)
*.csv
!EasyPIM/config/*.json
!EasyPIM/Documentation/**/sample*.csv

# Session memory file (do not commit persistent AI session state)
session_starter.md

# Agent configurations (personal workspace setup - don't commit)
.agents/
.github/agents/

# Agent plans (optional - commit if you want historical record)
# Uncomment to ignore:
plans/
# Or keep plans/ committed for audit trail

# PR description files (use .github/pull_request_templates/ for templates)
PR-DESCRIPTION.md
pr-description.md
PULL_REQUEST_TEMPLATE.md

# Local Pester cache / transient files
tests/*.cache
*.Coverage.xml

# OS / editor noise
*.swp
*.DS_Store
Thumbs.db

# PowerShell build artifact leftovers
*.psd1.tmp

# Debug and temporary files
debug-*.ps1
test-*.ps1
temp-*.ps1
temp-*.json
fix-*.ps1


# Ensure newline normalization (see .gitattributes)
session_starter.md
PostHog-Dashboard-Guide.md
EasyPIM/functions/Show-PIMReport.ps1.backup
QUICKSTART-AGENTS.md
BATCH3-SUMMARY.md
